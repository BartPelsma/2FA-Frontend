name: CI/CD Pipeline

on:
  push:
    branches:
      - main # of de naam van je hoofdbranch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14

    - name: Install dependencies
      run: npm install

    - name: Build and test
      run: npm run build

    - name: Deploy to production
      if: success() # Deploy alleen als de build en test succesvol zijn
      run: |
        # Voeg hier je deploy-script toe, bijvoorbeeld het kopiëren van bestanden naar een server.
        # Voorbeeld: scp -r build/ user@server:/path/to/destination

